import react from 'react';
import { withRouter } from 'react-router';

function NotFound() {
    
    return (
        <div>
            NotFound
        </div>
    );
}

export default withRouter(NotFound);